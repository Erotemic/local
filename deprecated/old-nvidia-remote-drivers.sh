# Get Information

# dpkg -l

# grep -R nvidia /etc/modprobe.d


# Remove Old Drivers
#apt-get remove nvidia-current
#apt-get remove nvidia-settings

#apt-get remove nvidia-current-updates
#apt-get remove nvidia-settings-updates

apt-get remove nvidia-current nvidia-settings nvidia-current-updates nvidia-settings-updates


# Install Correct Driver
#sudo apt-get install nvidia-current
